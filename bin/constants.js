"use strict";

const TRACE = "TRACE",
      DEBUG = "DEBUG",
      INFO = "INFO",
      WARNING = "WARNING",
      ERROR = "ERROR",
      FATAL = "FATAL",
      DEFAULT_LOG_LEVEL = INFO;

module.exports = {
  TRACE,
  DEBUG,
  INFO,
  WARNING,
  ERROR,
  FATAL,
  DEFAULT_LOG_LEVEL
};
