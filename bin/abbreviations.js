"use strict";

const { LOG_LEVEL_OPTION, FILE_PATH_OPTION, PACKAGE_NAME_OPTIONS } = require("./options");

const l = LOG_LEVEL_OPTION,
      f = FILE_PATH_OPTION,
      p = PACKAGE_NAME_OPTIONS;

module.exports = {
  l,
  f,
  p
};
