"use strict";

function verifyMetaSublemma(metaproofNode, premiseMetastatementNodes, conclusionMetastatementNode, context) {
  let metaSublemmaVerified = false;

  debugger

  return metaSublemmaVerified;
}

module.exports = verifyMetaSublemma;
